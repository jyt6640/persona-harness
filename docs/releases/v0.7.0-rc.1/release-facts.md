# v0.7.0-rc.1 Release Facts

## Release-Prep State

`0.7.0-rc.1` is an authorized next-channel release-prep candidate, not a
published package.

- Package metadata: `0.7.0-rc.1`.
- Exact release-prep base: `18c62da74d56518e2356711a38bb0a27406d9b10`.
- Intended publish channel after separate QA release GO: `next`.
- No npm publish, Git tag, GitHub Release, or dist-tag movement is included.
- Pre-publish registry baseline: `persona-harness@latest=0.6.0` and
  `persona-harness@next=0.6.0-rc.4`.

The baseline does not establish `0.7.0-rc.1` registry behavior. Registry
gitHead, shasum, integrity, dist-tag, tag, and GitHub Release facts must be
recorded only after the approved publish and External registry verification.

## Accepted Scope Since Stable `0.6.0`

- P0-1: host-neutral prepared-project `ph go` with transactional recovery
  limited to cooperative local writers and ordinary cooperative workspace edits.
- P0-2: substantive report evidence before transition, one truthful finish
  action plus at most one phased command, and plaintext/JSON loop parity.
- P0-3: narrow root public discovery, preserved direct paths/dev aliases, and
  invalid smoke/feedback writer safety.
- Item 12: package/init retirement of exactly 28 diff-rules assets without
  deleting the repository corpus.
- Item 13/24: Java-first front-door cleanup, advanced-surface isolation, and
  the public QUICK-DEMO three-beat path.
- Item 11/21: Java Iron List matrix plus three packaged warn-only,
  nonblocking precision conventions; no full Iron List enforcement.
- Item 19: POSIX Java/Spring/Gradle fixed-argv CI evidence reverification with
  bounded redacted artifacts and no automatic cleanup/revert behavior.
- Item 25: PH-core/language-pack/adapter ownership design only.

The post-stable accepted records retain their own exact-main QA and
local-current package-smoke provenance. Those local/current package facts do
not become `0.7.0-rc.1` registry evidence through this release-prep document.

## Package Boundary

The candidate continues the established package policy:

- package-visible public docs and durable release records are covered by the
  existing `docs/current/release` and `docs/releases` package entries;
- diff-rules and dormant non-Java programming references remain excluded;
- the Java precision conventions and Item 19 compiled CLI modules remain in the
  package surface;
- no package-file policy expansion is introduced solely for discoverability.

## Parked Boundaries

- `runtimeInjection=false`; no hook requirement.
- Item 20 hook work is PARK.
- Item 23 warn-to-block promotion is NO-GO.
- Item 27 non-Java packs are PARK.
- No `finish --json`, schema/evidence-schema/default movement, or release
  publication is included.

## Claim Boundary

This prep record does not prove CI correctness, generated-app certification,
product efficacy, token saving, app quality, security, broad reliability, or
broad enforcement.
