# v0.3.0 BootJar Guidance Fresh ON Review

## Goal

Verify whether the tightened Gradle/Spring guidance prevents the repeated executable Spring Boot workaround:

- do not add `bootJar { enabled = false }`;
- do not treat `:bootJar SKIPPED` as a valid executable Spring Boot build pass;
- fix Spring Boot plugin / Gradle wrapper / JDK toolchain compatibility instead of disabling `bootJar`.

This is a fresh Persona Harness ON run only. It is not a product-quality certification and not a broad A/B rerun.

## Run Source

- External test package: `/Users/yongtae/Desktop/blackbear-persona-harness-test`
- Fresh run: `/Users/yongtae/Desktop/blackbear-persona-harness-test/fresh-runs/01-book-loans/A-persona-on/bootjar-guidance-20260622-005742`
- Generated project: `/Users/yongtae/Desktop/blackbear-persona-harness-test/fresh-runs/01-book-loans/A-persona-on/bootjar-guidance-20260622-005742/generated`
- Fixture: `01-book-loans`
- Mode: Persona Harness ON
- Model: `openai/gpt-5.4-mini-fast`

## Harness Setup

The clean project was prepared with:

```bash
npm install -D /Users/yongtae/Desktop/persona-harness
npx ph init
npx ph intake --force
npx ph policy init --force
npx ph plan --force
npx ph plan --accept
```

OpenCode then ran against the generated project.

## Observed Build Behavior

The generated `build.gradle` used:

- Spring Boot plugin `3.3.6`;
- Java toolchain `21`;
- JUnit Platform launcher in `testRuntimeOnly`;
- no `bootJar { enabled = false }`;
- no `tasks.named("bootJar") { enabled = false }`.

Independent verification:

```text
gradle build: BUILD SUCCESSFUL
> Task :bootJar UP-TO-DATE
```

No `:bootJar SKIPPED` workaround appeared in the generated build.

## Manual QA

Independent HTTP smoke ran with `gradle bootRun --args='--server.port=8092'`.

Observed status codes:

```text
POST /books: 201
GET /books: 200
POST /loans: 201
duplicate POST /loans: 400
```

## Package Flow Check

The generated Java source used the expected domain-first layer flow:

- `book/application`
- `book/application/dto/command`
- `book/application/dto/result`
- `book/domain`
- `book/infrastructure`
- `book/presentation`
- `book/presentation/dto/request`
- `book/presentation/dto/response`

Repository boundary:

- domain ports: `BookRepository`, `LoanRepository`;
- infrastructure implementations: `InMemoryBookRepository`, `InMemoryLoanRepository`.

The run did not create `controller`, `service`, or `repository` role-name packages.

## Service Storage Check

Application services did not directly own `Map`, `AtomicLong`, `nextId`, `idCounter`, or `sequence` state.

Storage state and ID sequence stayed inside infrastructure repositories.

## Evidence

- `.persona/evidence`: 44 files generated.
- Implementation report marked `Status: filled`.
- Implementation report explicitly recorded:
  - `gradle build` successful;
  - `:bootJar` executed, not skipped;
  - `bootJar` should not be disabled to pass build.

## Decision

The narrow bootJar disable regression is not reproduced in this fresh ON run.

The package-flow guidance also held in this run, so the previous `buildable-package-flow-mismatch` blocker is reduced for this fixture.

## Limitations

- Single fresh ON run.
- No paired OFF rerun in this loop.
- Not a generated application product-quality guarantee.
- Not an enforcement gate.
- Not proof that all future models will keep `bootJar` enabled.

## Next

Move to a release/demo packaging decision loop only if the project accepts this single fresh ON result as sufficient for the current alpha track.

If release confidence needs stronger evidence, run one additional fresh ON smoke on a different small backend domain before changing packaging status.
