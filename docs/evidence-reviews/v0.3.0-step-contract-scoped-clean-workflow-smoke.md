# v0.3.0 Step Contract Scoped Clean Workflow Smoke

## Goal

Run one clean Java/Spring workflow smoke after scoping the old step API contract rules to roomescape fixtures, then verify that a new project can use README/profile status and body contracts without old roomescape 200 OK pressure.

## Setup

- Persona Harness package: `persona-harness@0.2.1`
- Install mode: local tarball from the current repo
- Clean project: `/tmp/persona-step-contract-smoke-G3oAmV`
- Model: `openai/gpt-5.4-mini-fast`
- Fixture: Team Task Board API
- Workflow:
  - `git init`
  - `npm install -D /Users/yongtae/Desktop/persona-harness/persona-harness-0.2.1.tgz`
  - `./node_modules/.bin/persona-harness init`
  - `./node_modules/.bin/persona-harness intake`
  - fill `.persona/project-profile.jsonc`
  - `./node_modules/.bin/persona-harness plan`
  - `./node_modules/.bin/persona-harness plan --accept`
  - OpenCode implementation

## Profile

- project-context: `team`
- project-goal: `production-service`
- project-scale: `medium`
- application-type: `rest-api`
- storage: `database`
- persistence-technology: `jdbc-template`
- migration-style: `flyway`
- package-style: `domain-first`
- architecture-style: `clean-architecture-light`
- boundary-strictness: `strict`

Profile note:

> Controller status/body must follow the README API contract: create endpoints return 201, list endpoints return 200, invalid state returns 409, and missing resources return 404.

## Generated Shape

The generated app used:

- Gradle only; no `pom.xml`.
- Root package: `com.example.teamtaskboard`.
- Domain-first packages for `member` and `task`.
- `presentation`, `application`, `domain`, and `infrastructure` layers.
- Domain repository ports:
  - `member/domain/MemberRepository.java`
  - `task/domain/TaskRepository.java`
- Infrastructure repository implementations:
  - `member/infrastructure/JdbcMemberRepository.java`
  - `task/infrastructure/JdbcTaskRepository.java`
- Class-based domain models:
  - `member/domain/Member.java`
  - `task/domain/Task.java`
- Flyway migration:
  - `src/main/resources/db/migration/V1__init.sql`

## Status / Body Contract Result

README/profile contract was preserved:

| Endpoint | Expected | Observed |
| --- | --- | --- |
| `POST /members` | `201 Created` | `201` |
| `GET /members` | `200 OK` | `200` |
| `POST /tasks` | `201 Created` | `201` |
| `GET /tasks` | `200 OK` | `200` |
| `POST /tasks/{taskId}/assignment` | `200 OK` | `200` |
| `PATCH /tasks/{taskId}/status` | `200 OK` | `200` |
| completed task -> `IN_PROGRESS` | `409 Conflict` | `409` |
| missing member | `404 Not Found` | `404` |

This is the key signal for this loop: the generated Controller used README/profile status contracts instead of old roomescape step fixture status rules.

## Step Contract Scope Check

Runtime evidence:

- `.persona/evidence/phase0` contained 3 bootstrap evidence files.
- All 3 evidence entries targeted `README.md`.
- Selected rule was `backend/java-backend-bootstrap.md`.
- `backend/step1-api-contract.md`: 0 evidence hits.
- `backend/step2-3-api-contract.md`: 0 evidence hits.

Installed-package injection check against generated Java targets:

| Target | Step contract selected |
| --- | --- |
| `member/presentation/MemberController.java` | no |
| `task/presentation/TaskController.java` | no |
| `member/presentation/dto/request/MemberCreateRequest.java` | no |
| `task/presentation/dto/response/TaskResponse.java` | no |

## Verification

OpenCode ran:

- `gradle test`: passed after adjusting toolchain to local JDK 21.
- `gradle build`: passed after choosing a Spring Boot plugin compatible with local Gradle 9.4.
- `gradle bootRun`: ran successfully.
- HTTP happy path: passed.
- HTTP failure path: passed.
- `npx ph plan --report-filled implementation`: passed.
- `npx ph plan --report-filled review`: passed.

Independent verification repeated after OpenCode:

- `gradle test`: passed.
- `gradle build`: passed.
- `gradle bootRun`: ran successfully.
- Manual `curl` smoke observed `201`, `200`, `409`, and `404` as expected.

## Notes

- The first local tarball attempt was invalid because `npm pack` was run before `dist` existed. The smoke was rerun after sequential `npm run build` then `npm pack`, and the tarball was checked for `package/dist/cli/index.js`.
- The generated project initially used Java 17 toolchain on a JDK 21 machine; OpenCode corrected it to Java 21.
- The generated project initially hit the known `CopyProcessingSpec.getDirMode()` Gradle/Spring Boot mismatch; OpenCode corrected the Spring Boot plugin line and kept `bootJar` enabled.
- This is smoke evidence, not generated app product-quality certification.

## Decision

PASS.

The step API contract scoping change held in a clean Java/Spring workflow. README/profile status contracts were preserved, and old roomescape step contract rules did not appear in generated clean-project Java target selection.

## Next

Move to the next v0.3 productization surface. The strongest next candidate is philosophy/policy overlay design, because backend clean workflow smoke now has enough evidence that README/profile guidance can shape project generation without roomescape step contract bleed-through.
