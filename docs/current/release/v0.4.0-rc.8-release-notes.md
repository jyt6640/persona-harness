# v0.4.0-rc.8 Release Notes

## Version

`v0.4.0-rc.8`

## Dist Tag

`next`

`0.4.0-rc.8` is published under the next-channel for the opt-in TDD Workflow
Rail. `latest` did not move.

Registry verification:

- `persona-harness@next`: `0.4.0-rc.8`;
- gitHead: `18a9bb2f4a9706e4115ffff5d9e864934cd9f0bd`;
- shasum: `fc4de25901d4c678799ea66b8e63293dc5f46a12`;
- dist-tags: `next=0.4.0-rc.8`, `latest=0.3.9-alpha.8`,
  `alpha=0.3.9-alpha.8`;
- tag: `v0.4.0-rc.8` points at
  `18a9bb2f4a9706e4115ffff5d9e864934cd9f0bd` after registry gitHead
  verification.

## Summary

This candidate packages the QA/External-accepted TDD Workflow Rail:

- `enforce.tdd` remains default-off;
- `ph workflow test` records red evidence only when strict
  `enforce.executeVerification=true` lets PH directly run Gradle/JUnit and
  observe a testcase `<failure>`;
- strict-off TDD is advisory/unavailable and writes no fake red/green evidence;
- compile/no-JUnit failure and JUnit `<error>` do not count as valid red;
- workflow closure can block archive/finish with `tdd-red-evidence-missing` or
  `tdd-not-red-then-green`;
- `ph workflow check`, `ph workflow archive <ticket>`, and
  `ph workflow finish implement` can record green evidence for the same
  ticket/test id after red evidence.

## Package-Surface Evidence

TDD Workflow Rail External local/current tarball smoke PASS:

- source HEAD: `b5d98e1286c5a7f0349af8a461efdf2ce84a653e`
  (`b5d98e1 feat(cli): add tdd workflow rail`);
- source type: fresh local/current tarball only, not registry `@next`;
- package/version at smoke time: `persona-harness@0.4.0-rc.7`;
- archive:
  `/Users/yongtae/Desktop/persona-harness-artifacts/archive/2026-06-24-desktop-persona-runs/tdd-workflow-rail-b5d98e1-20260701-164350`;
- tarball:
  `/Users/yongtae/Desktop/persona-harness-artifacts/archive/2026-06-24-desktop-persona-runs/tdd-workflow-rail-b5d98e1-20260701-164350/persona-harness-0.4.0-rc.7.tgz`;
- npm shasum: `6665daa2fc01d01cbc8095bddadffc7654589572`;
- sha256:
  `7262e64d80d713f6d124a37fec0a4fb7762be22ab0f2c6ae32e422d19f961360`;
- package entries included `dist/cli/workflow-tdd.js` and updated
  `dist/cli/closure-verification-runner.js`;
- installed runtime default remained `tdd:false`;
- strict-off/advisory wrote no red/green evidence;
- strict PH-run Gradle/JUnit `<failure>` recorded red evidence with
  `execution=ph-direct-gradle-junit`, `generatedBy=persona-harness`, test id,
  JUnit snapshot, and digest;
- compile/no-JUnit failure and JUnit `<error>` wrote no red evidence;
- green-only/no-red blocked test/finish/archive with
  `tdd-red-evidence-missing`;
- hand-written minimal evidence was ignored;
- red-to-green for the same ticket/test id passed `workflow check`,
  `workflow archive req-1`, and `workflow finish implement` in an isolated
  fixture.

External registry-only smoke PASS:

- source: registry `persona-harness@next`, installed as `0.4.0-rc.8`;
- registry gitHead: `18a9bb2f4a9706e4115ffff5d9e864934cd9f0bd`;
- registry shasum: `fc4de25901d4c678799ea66b8e63293dc5f46a12`;
- archive:
  `/Users/yongtae/Desktop/persona-harness-artifacts/archive/2026-06-24-desktop-persona-runs/rc8-registry-tdd-workflow-rail-20260701-193324`;
- work root: `/tmp/persona-rc8-registry-tdd-workflow-rail-20260701-193324`;
- package entries included `dist/cli/workflow-tdd.js` and
  `dist/cli/closure-verification-runner.js`;
- default generated config omits `enforce.tdd`; installed runtime default
  remains `tdd:false`;
- strict-off/advisory with `enforce.tdd=true` and
  `executeVerification=false` exited 0 and wrote no red/green evidence;
- strict PH-run Gradle/JUnit `<failure>` recorded red evidence with
  `execution=ph-direct-gradle-junit`, `generatedBy=persona-harness`, test id,
  JUnit snapshot, and digest;
- compile/no-JUnit failure and JUnit `<error>` wrote no red evidence;
- green-only/no-red blocked `workflow test`, `workflow finish implement`, and
  `workflow archive req-1` with `tdd-red-evidence-missing`;
- hand-written minimal forged evidence was ignored and finish blocked;
- red-to-green for the same ticket/test id passed `workflow test`,
  `workflow check`, `workflow archive req-1`, and `workflow finish implement`.

## Boundaries

This is next-channel registry package/runtime surface evidence only.

Do not claim:

- model/OpenCode/eval evidence;
- token-saving or provider-token saving;
- product quality, generated-app certification, test-sufficiency proof, broad
  reliability, or closure guarantee;
- a full TDD framework;
- test scaffolding, coverage, or mutation testing.
