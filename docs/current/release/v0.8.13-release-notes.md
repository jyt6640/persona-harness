# v0.8.13 Release Notes

## Current package authority

`0.8.13` is the next package authority after the immutable published `0.8.12`
release. The root package, lockfile, private Persona shared-skills package, and
`consumer-authority-v0813-acceptance.json` bind the same version. The v0.8.12
record remains strict historical evidence and cannot authorize this candidate.

The authority fetch keeps the explicit verified artifact tuple and publishes
`consumer-authority-fetch.4`, adding only a finite nonreflective `sourceReason`
when a terminal source binding result is returned. Other binding reasons remain unchanged; no receipt, path, digest,
certificate, artifact, credential, or raw diagnostic is retained or exposed.

## Release boundary

This branch prepares version authority only. Fresh Source and Package gates
must bind the exact candidate and canonical tar before any release action; the
future V4 fixture observation remains a separate hosted residual.
