# v0.8.0-beta.24 Release Notes

## V4 Final Observer Procedure

This package-visible procedure-only candidate advances the governed
final-observer record to `0.8.0-beta.24`. It changes no Persona Harness
authority, Fetch, Finish, artifact transport, workflow runtime, registry
channel, tag, release, or fixture behavior.

`0.8.0-beta.23` is non-reusable procedure evidence. Its raw empty
`git status --porcelain` and `git clean -ndx` requirement contradicted the
approved post-commit runtime and build residue needed to prepare the same
consumer. That contradiction is a procedure defect, not a product result, and
must not be retried. The reviewed V4 procedure record is bound by SHA-256
`5389c027b21f72f325a5d9e467ecd4d150f672e14da1d04f51774602a284c57d`.
No local absolute path is a source of truth.

The package-visible
[`consumer-authority-beta24-acceptance.json`](consumer-authority-beta24-acceptance.json)
is the strict record for the next one-shot observation.

## Required V4 Cleanliness

The retained consumer must preserve the same canonical CWD, Git top-level,
HEAD, remote parent, reusable pin, and source digest map at baseline,
source-bound preparation, credential handoff, observer child, and immediately
before push. The final commit may contain only documented source-bound
bootstrap paths plus the immutable reusable pin. It must never include a
runtime/build residue, a tracked source drift, or an unlisted path.

Each fence compares two views with the exact stage-specific residue set:

- the NUL-safe `git status --porcelain=v1 -z --untracked-files=all
  --ignored=matching` enumeration is the authoritative residue view; and
- the normalized `git clean -ndx` diagnostic must describe the same set.

The only permitted post-commit residue classes are `.gradle/**`,
`.persona/.ph-init-manifest.json`, `.persona/evidence/**`,
`.persona/workflow/**`, `build/**`, and `node_modules/**`. Every residue and
ancestor is checked with `lstat` and `realpath` containment. `.local/**`,
`.config/**`, and `.cache/**` are unconditionally rejected; aliases,
replacement, tracked modification, unexpected residue, or source-identity
drift abandon the consumer before any live fetch.

The evaluator owns the exact projection: baseline permits no residue;
source-bound preparation permits only the init manifest and workflow state;
credential handoff, observer child, and immediately-before-push permit the
six documented runtime/build roots. A caller cannot supply a wider projection
or a nested residue path.

Every host credential, `gh`, Git, Node, and npm process still uses `env -i`
and explicit absolute tool paths. All fifteen external host-state roots remain
outside the consumer realpath. The credential is handed only to the fixed
read-only observer child. Slow preparation completes before the one normal
push; no arbitrary outer timeout may relax the online certificate-validity
deadline.

## Complete Local Boundary

The source-built and fresh packed-installed contracts exercise the V4 five
fences, valid constrained residue projections, tracked/unexpected/forbidden
residue rejection, alias containment, source-identity drift, the public
Java/Spring readiness route, privacy-safe observer preflights, modeled
trusted/unconsumed authority, one Finish consumption, and immediate replay
block. Those modeled results do not substitute for a current original artifact
or online cryptographic verification.

## Remaining Hosted Evidence

After the normal gates, the sole remaining action is one separately authorized
V4 fixture commit and normal push. The already prepared same consumer then
immediately acquires and verifies current original bytes online before the
leaf certificate `notAfter`, performs one authenticated fetch, consumes Finish
once, and verifies immediate replay rejection. It cannot bootstrap, reset,
switch consumers, change CWD/HEAD/source/profile, or replace its home/store
after fetch.
