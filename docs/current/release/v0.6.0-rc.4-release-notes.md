# v0.6.0-rc.4 Release Notes

## Version

`v0.6.0-rc.4`

## Dist Tag

Planned dist-tag: `next`.

This is release prep only. It has not been published, tagged, or assigned a
dist-tag. Until a separate publish task succeeds, npm registry state remains:

- `persona-harness@latest=0.5.0`;
- `persona-harness@next=0.6.0-rc.3`;
- `persona-harness@alpha=0.3.9-alpha.8`.

The current published `0.6.0-rc.3` registry package came from gitHead
`e1af520cf000e805e7df6a1616906f3f9b0e4976` with shasum
`ef498adfac138d9d0843406cba53acf76b34c6f1` and integrity
`sha512-nXImwyxON5zoph8Y3LyXqJhW1NIcX+3JvsBSFAdQlersZ8zO1KmRuRJVKB1DF87fbYxDpVyxMqFU1C6sA8HJQQ==`.

## Stable Deferral

Stable `0.6.0` is not being prepared in this release. HARDEN-2 remains blocked
because the prerequisite stable gate is not met: `0.6.0` stable is not
published and external tester recruitment for stable has not started.

H1-6a repeated-output compression failed the real rail-entry precheck:

- control/current rail entry: `3/5`;
- Summary-header candidate rail entry: `1/5`;
- delta: `-40pp`;
- non-inferiority: not met.

Compression remains NO-GO and unimplemented. `0.6.0-rc.4` is a prerelease
`next` candidate pending QA publish-readiness, not a stable release or default
movement.

## Summary

`0.6.0-rc.4` prepares the accepted HARDEN-1 hardening after the published rc3
line while keeping the product center gate-first: deterministic finish/closure
gates, PH-generated evidence, read-only status/report surfaces, and
default-off continuation previews.

This prep does not claim product efficacy, token/provider-token saving,
navigation benefit, app quality, full-TDD/test sufficiency, broad reliability,
closure guarantee, autonomous completion, generated-app certification,
deterministic enforcement, production-ready delegation, reliable automatic
subagent orchestration, automatic completion/downgrade/removal, CodeGraph/LSP
default/effectiveness, or broad product behavior.

## Included Since rc3

- H1-0 preflight PARTIAL: the rc3 final docs record closed and Stage 18
  tokens-per-verified-completion telemetry was recorded, but the real n>=5
  rail-entry regression gate initially remained unresolved and later failed for
  H1-6a compression.
- H1-1 blocker-step contract: workflow continuation loops stop on unmapped
  blockers before consuming retry/iteration budget, and failed-finish human
  output points to Persona Harness configuration/maintainer escalation instead
  of direct finish/check rerun loops.
- H1-3 deterministic blocker order and chain-depth contract, plus H1-2
  mechanical finish regression coverage for a tiny finish-reachable
  Java/Spring-shaped fixture.
- H1-4 block-level toolchain fail-closed behavior and human mapped toolchain
  follow-up. Built-in `controller.persistence-import` remains default `warn`;
  explicit block-level ast-grep/toolchain-dependent conventions add a walkable
  `convention-toolchain-missing` closure blocker when the toolchain is missing
  or fails.
- H1-5 atomic writes and fail-safe reads split by file family. This is not a
  repo-wide all-writes atomicity claim.
- H1-6a real precheck failed: repeated-output compression remains NO-GO and
  unimplemented.
- H1-6b structured finish summary derivation: failed
  `workflow finish implement` human summaries are derived from structured
  closure blocker/required-fix objects instead of reparsing rendered
  `Closure blocker:` text. Stage 20 summary shape, H1-1 escalation wording,
  and H1-4 mapped toolchain guidance are preserved.

## Registry Evidence Notes

This rc4 prep is not registry evidence. HARDEN-1 local-current package/runtime
records remain local-current evidence until a future publish includes the rc4
commit and passes registry gitHead/shasum verification plus External registry
smoke.

## Claim Audit

Do not claim:

- product efficacy, navigation benefit, token saving, or provider-token saving;
- app/product quality guarantee;
- full TDD framework, coverage, mutation testing, or test sufficiency;
- broad reliability, closure guarantee, autonomous completion, or generated-app
  certification;
- deterministic role enforcement or production-ready delegation;
- reliable automatic OpenCode subagent orchestration;
- automatic completion, downgrade, removal, or config mutation;
- CodeGraph/LSP default behavior or effectiveness.

Allowed wording:

- release prep for `0.6.0-rc.4` targeting `next`;
- stable remains `latest=0.5.0` and stable `0.6.0` is deferred;
- published `next` remains `0.6.0-rc.3` until rc4 is published;
- H1-6a compression remains NO-GO and unimplemented;
- H1-6b structured finish summary derivation is accepted;
- no publish/tag/dist-tag movement in this prep.
