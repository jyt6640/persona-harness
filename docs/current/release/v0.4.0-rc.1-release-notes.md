# v0.4.0-rc.1 Release Notes

## Version

`v0.4.0-rc.1`

## Dist Tag

`next`

`0.4.0-rc.1` is a release candidate for the workflow rail / closure planner
milestone. Do not move `latest` for this release candidate.

## Parked Draft Status

This release prep is parked and not publish-ready while CLI verifies whether
the read-only closure planner is wired into the active workflow rail:
`workflow continue`, `plan-next`, finish guidance, and post-build closure
guidance.

Prepared commit: `5edb535 chore(release): prepare 0.4.0-rc.1`.

Do not publish, tag, push, or move `latest` from this draft until the CLI wiring
result is reviewed.

## Summary

This release candidate packages the read-only workflow closure planner surface
as a user-facing workflow rail milestone candidate.

Valid claim:

- the read-only closure planner supported one registry Windows SSH
  implementation-to-finish proxy trial.

Boundaries:

- eval remains stopped;
- `docs/current/injection-value-status.json` remains
  `injection-effect-not-proven`;
- this is not eval proof, A/B proof, PH superiority proof, generated app quality
  certification, closure guarantee, or general reliability guarantee.

## Included Surface

- `workflow closure status --json` exposes workflow closure state and blockers.
- `workflow closure next --json` exposes deterministic next steps and the first
  actionable blocker.
- `workflow finish implement` remains the final authority.
- JSON state keys are stable, including `currentTicket: null` when no current
  ticket exists.
- Verification remains evidence-backed; command names alone are not PASS
  evidence.

Not included:

- no `closure run` executor;
- no report auto-fill;
- no ticket auto-archive;
- no workflow auto-finish;
- no finish gate weakening.

## Product Usability Signal

External Smoke completed one alpha7 Windows SSH registry
implementation-to-finish proxy trial:

- package source: registry `alpha`, installed
  `persona-harness@0.3.9-alpha.7`;
- Korean idea-first input was preserved;
- no-token rail completed draft, approve, split, next, continue, and check;
- closure planner surfaced stable JSON and first actionable blockers;
- OpenCode used the PH rail, implemented a Spring Boot/Gradle backend, filled
  implementation/review reports, ran report-filled commands, archived `req-1`,
  and handled the Java role read coverage blocker;
- final `workflow finish implement`: PASS;
- `gradlew.bat test`, `bootJar`, and `build`: `BUILD SUCCESSFUL`;
- runtime smoke observed create/update/delete success and 404/400 responses.

This is one product usability trial only. It does not establish broad
reliability or generated app quality.

## Verification

Release-prep verification for this worktree:

```bash
npm test
npm run typecheck
npm run build
npm run check:docs
npm run smoke:product-mvp
npm pack --dry-run
git diff --check
```

Observed result on 2026-06-29:

- `npm test`: passed, 70 files / 463 tests.
- `npm run typecheck`: passed.
- `npm run build`: passed.
- `npm run check:docs`: passed.
- `npm run smoke:product-mvp`: passed.
- `npm pack --dry-run`: passed for `persona-harness@0.4.0-rc.1`.
  - filename: `persona-harness-0.4.0-rc.1.tgz`;
  - package size: `390.1 kB`;
  - unpacked size: `1.6 MB`;
  - shasum: `ffa444ee828723ec3c10d306b4f92ce077967241`;
  - total files: `378`.
- `git diff --check`: passed.

## Publish Notes

- Publishing is paused while the prep is parked.
- Preferred publish command: `npm publish --tag next`.
- Do not move `latest`.
- If npm requires OTP/2FA, stop after the first failure and hand off the exact
  command for the user to run.
- Create and push `v0.4.0-rc.1` only after registry verification confirms
  `persona-harness@0.4.0-rc.1` exists with the release prep commit as gitHead.
