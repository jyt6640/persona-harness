# v0.3.8-alpha.0 Clean Workflow Reliability Smoke

Date: 2026-06-24
Source repo: `/Users/yongtae/Desktop/persona-harness`
Source commit: `2a76ee0`
Package: `persona-harness@0.3.8-alpha.0`

## Result

PASS with one follow-up gap.

The tarball-installed CLI worked in a clean project for bootstrap, requirement split, next-ticket selection, continuation, pending-ticket finish blocking, and technical-constraints archive-candidate guidance.

Observed gap: `npx ph workflow check` reported `PASS` and suggested history archive while a pending workflow ticket still existed. `npx ph workflow finish implement` correctly blocked final completion on the pending ticket.

## Clean Project Path

`/tmp/persona-harness-v038-clean-smoke-S4O63s`

## Install Method

Tarball install from local pack output:

- Pack directory: `/tmp/persona-harness-pack-ZXt2cv`
- Tarball: `/tmp/persona-harness-pack-ZXt2cv/persona-harness-0.3.8-alpha.0.tgz`
- Install command: `npm install -D /tmp/persona-harness-pack-ZXt2cv/persona-harness-0.3.8-alpha.0.tgz`
- Installed version evidence: `npm ls persona-harness` showed `persona-harness@0.3.8-alpha.0`

## Commands

```sh
npm pack --pack-destination /tmp/persona-harness-pack-ZXt2cv
mkdir /tmp/persona-harness-v038-clean-smoke-S4O63s
cd /tmp/persona-harness-v038-clean-smoke-S4O63s
npm init -y
npm install -D /tmp/persona-harness-pack-ZXt2cv/persona-harness-0.3.8-alpha.0.tgz
npx ph --help
npx ph bootstrap backend
npx ph workflow split README.md
npx ph workflow next
npx ph workflow continue
npx ph workflow finish implement
npx ph workflow archive step-1
npx ph workflow archive step-2
npx ph workflow check
npx ph workflow finish implement
opencode run "README.md 보고 구현해줘"
```

OpenCode command was bounded to 60 seconds and timed out.

## Workflow Split / Next

README use case: Equipment Loan Backend, a small Java/Spring Gradle backend with these sections:

- Step 1. Equipment catalog API
- Step 2. Loan checkout and return API
- Step 3. Technical Constraints

`npx ph workflow split README.md` output:

```text
Workflow split complete.
Source: README.md
Source kind: file
Requirements analysis: .persona/workflow/requirements-analysis.md
Backlog: .persona/workflow/backlog.md
Tickets created: 3
```

`npx ph workflow next` output included:

```text
Ticket: step-1
Title: Equipment catalog API
Card: .persona/workflow/work/step-1/00-task-card.md
Path: .persona/workflow/work/step-1/00-task-card.md
```

## Workflow Continue Output

Initial `npx ph workflow continue` showed the pending ticket path:

```text
Pending workflow ticket:
Ticket: step-1
Title: Equipment catalog API
Path: .persona/workflow/work/step-1/00-task-card.md
Next command: npx ph workflow next
If complete: npx ph workflow archive step-1
```

After archiving `step-1` and `step-2`, `workflow continue` showed:

```text
Pending workflow ticket:
Ticket: step-3
Title: Technical Constraints
Path: .persona/workflow/work/step-3/00-task-card.md
Next command: npx ph workflow next
If complete: npx ph workflow archive step-3
```

Continuation evidence also preserved that only Step 1 was represented as completed and Step 2/3 remained pending.

## Finish Pending-ticket Output

With all tickets still pending, `npx ph workflow finish implement` exited `1` and included:

```text
Pending workflow tickets remain: step-1, step-2, step-3.
Run `npx ph workflow next` to resume the next ticket.
  Ticket: step-1
  Title: Equipment catalog API
  Path: .persona/workflow/work/step-1/00-task-card.md
  Next command: `npx ph workflow next`
  If this ticket is complete: `npx ph workflow archive step-1`
```

The same output listed `step-2` and `step-3` with title/path/archive commands.

## Technical Constraints Candidate Output

After creating Java/Gradle stack markers, filling implementation/review reports, recording profile/read evidence, and archiving `step-1` and `step-2`, `npx ph workflow check` showed PASS signals:

```text
Workflow status: PASS
.persona/workflow/plan.md: accepted
.persona/workflow/implementation-report.md: filled
.persona/workflow/review-report.md: filled
.persona/evidence: present
read coverage: README ranges observed
profile read coverage: project profile ranges observed
command discipline: bearshell observed
stack alignment: profile expects Java/Spring/Gradle and generated project has Gradle + src/main/java
```

Then `npx ph workflow finish implement` still exited `1` because `step-3` remained pending and printed:

```text
Ticket: step-3
Title: Technical Constraints
Path: .persona/workflow/work/step-3/00-task-card.md
Next command: `npx ph workflow next`
If this ticket is complete: `npx ph workflow archive step-3`
Technical constraints note: may already be satisfied by workflow reports filled, README/read coverage PASS, project profile read coverage PASS, Java/Spring Gradle stack alignment PASS, bearshell command discipline PASS, workflow evidence present.
Archive only after review: `npx ph workflow archive step-3`
```

This confirms the ticket is not auto-archived.

## OpenCode Smoke

OpenCode was installed and `opencode --help` worked. A bounded run was attempted from the clean project:

```sh
opencode run "README.md 보고 구현해줘"
```

Result: timeout after 60 seconds, no stdout/stderr captured. This is inconclusive and is not counted as pass evidence.

## Pass/Fail

PASS:

- Tarball install into a clean npm project.
- `npx ph bootstrap backend`.
- `workflow split README.md` produced three tickets.
- `workflow next` showed ticket id/title/card/path.
- `workflow continue` showed pending ticket id/title/path and archive command.
- `finish implement` blocked on pending tickets with id/title/path/archive guidance.
- Technical constraints ticket stayed pending and became a review/archive candidate when PASS-like signals existed.
- Profile read and Java/Spring Gradle stack alignment gates stayed green in the PASS-signal fixture.
- Continuation output did not encourage claiming completion after only Step 1; it showed pending ticket state.

FAIL / gap:

- `workflow check` can report `PASS` and suggest history archive while pending tickets remain. `finish implement` blocks correctly, but `workflow check` status/next text is weaker than the final gate.
- OpenCode smoke timed out and is inconclusive.

## Gaps

1. Consider adding pending-ticket awareness to `workflow check` so its status/next action does not suggest archive while backlog still has pending tickets.
2. OpenCode smoke needs a longer/manual authenticated run if HQ wants model-driven implementation evidence.

## Commit

Report-only commit candidate:

```text
docs: add v0.3.8 clean workflow smoke report
```
