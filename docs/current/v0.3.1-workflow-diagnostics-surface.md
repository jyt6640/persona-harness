# v0.3.1 Workflow Diagnostics Surface

## Goal

Make the local alpha easier to verify after install and harder to misuse from a short OpenCode TUI prompt.

The command surface is intentionally AI-facing. A human should be able to say "README 보고 구현해줘" or "계획대로 구현해줘"; the agent should then discover and run the relevant `npx ph ...` workflow commands instead of making the human remember every step.

## Problem

Recent TUI use showed a useful split:

- Java/Spring Gradle product-code shape was often influenced in the right direction.
- Workflow evidence discipline was weaker: the agent could skip `.persona/workflow/implementation-report.md` and use raw shell instead of `npx ph bearshell`.

That is not primarily a Java Clean Code rule gap. It is a command-surface and workflow reminder gap.

## Added Surface

These commands are stable handles for OpenCode/Codex-style agents to call:

- `ph workflow check`
  - Reports plan/report/evidence status.
  - Prints the next workflow action.
  - Report-only; not a product-quality gate.

- `ph workflow start implement`
  - Checks the accepted-plan implementation gate.
  - Prints the AI-facing implementation rail: `plan --implement`, read context, implement, use `bearshell`, fill reports, and finish.
  - Reuses the lower-level implementation guard.

- `ph workflow finish implement`
  - Blocks final answer until the implementation report, review report, evidence, and command-discipline workflow state are complete.
  - Reuses the lower-level final guard.
  - Does not inspect generated app quality.

- `ph workflow guard implement` / `ph workflow guard final`
  - Lower-level strict gates used by the workflow rails.
  - Kept for direct diagnostics and script composition.

- `ph doctor`
  - Reports local Node/npm/OpenCode and Persona Harness integration state.
  - Checks `.opencode/opencode.json`, plugin path signal, `.persona/harness.jsonc`, `.persona/rules`, `.persona/workflow/plan.md`, and `.persona/evidence`.
  - Checks public npm dist-tags for `alpha` and `latest` when the registry is reachable; reports `unavailable` instead of failing when it is not.

- `ph smoke`
  - Writes `.persona/workflow/smoke-report.md`.
  - Summarizes the current workflow state for external tester reproduction.

- `ph feedback`
  - Writes `.persona/workflow/feedback-report.md`.
  - Gives external testers a stable feedback template.

- `ph review backend-shape`
  - Writes `.persona/workflow/backend-shape-report.md`.
  - Observes Gradle-only shape, layer/package structure, domain repository port, infrastructure repository adapter, service-owned storage/id sequence, domain behavior, DTO boundary, bootJar status, and verification report evidence.

- `ph evidence summary`
  - Writes `.persona/evidence/summary.md`.
  - Summarizes raw evidence by role, target file name, selected rule, and selected shared skill.
  - Keeps raw evidence untouched and gives humans a smaller first-read surface.

- `ph history`
  - Keeps archiving active workflow artifacts without deleting them.
  - Includes `.persona/evidence/summary.md` content in archive `summary.md` when the evidence summary exists.

## Prompt/Plan Adjustment

`ph plan --prompt`, `ph plan --implement`, and `ph workflow guard` now make the short TUI route more explicit:

- short requests such as "README 보고 구현해줘" should first run `npx ph workflow start implement`;
- shell verification should prefer `npx ph bearshell`;
- after implementation, the agent should fill the implementation report and run `npx ph plan --report-filled implementation`;
- before the final answer, the agent should fill the review report after manual QA, run `npx ph plan --report-filled review`, and pass `npx ph workflow finish implement`.

Human-facing wording should stay short. The stronger long prompt remains available as a fallback when the agent misses the workflow, but it is not the desired everyday UX.

Injection block dieting is intentionally narrow:

- selected shared-skill metadata still records the package reference path;
- model-facing injection text no longer repeats the full shared-skill file path;
- selected rule paths remain visible because they are the current rule-catalog evidence surface.

## Non-Goals

- Not a generated app product-quality certification.
- Not rule enforcement.
- Not AST/linter/Guard.
- Not frontend/infra productization.
- Not a TDD workflow implementation yet.

## Next Candidate

The first clean TUI smoke was partially positive: short implementation requests used `ph plan --implement`, `ph workflow check`, `ph bearshell` for later test/build verification, and implementation report filling, but the review report stayed `template`.

The follow-up clean TUI smoke after stricter review wording was stronger:

- `README 보고 구현해줘` triggered `ph plan --implement`;
- implementation report was filled and marked with `ph plan --report-filled implementation`;
- review report was filled and marked with `ph plan --report-filled review`;
- final `ph workflow check` reported `PASS`;
- evidence summary contained project-bootstrap and Service target evidence.

Remaining limitations:

- the agent still used raw shell instead of `npx ph bearshell` for several checks in the follow-up smoke;
- `ph workflow check` now surfaces that command-discipline gap as a report-only WARN when filled workflow reports mention raw shell use or omit `npx ph bearshell`;
- backend-shape matcher noise around `*Store.java` adapters and split verification-report text was fixed after the follow-up smoke.

Next candidate:

1. Package the v0.3.1 alpha candidate if report-only command-discipline WARN is acceptable.
2. Do not make `bearshell` a build/test gate before external tester feedback.
