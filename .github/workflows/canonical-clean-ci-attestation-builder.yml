name: Canonical clean CI attestation builder

on:
  push:
    branches:
      - main

concurrency:
  group: canonical-clean-ci-attestation-builder-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write
  attestations: write
  artifact-metadata: write

jobs:
  build:
    name: Build canonical clean CI finish attestation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build canonical receipt and predicate
        run: node scripts/build-clean-ci-attestation.mjs

      - name: Sign canonical finish predicate
        id: attest
        uses: actions/attest@ce27ba3b4a9a139d9a20a4a07d69fabb52f1e5bc
        with:
          subject-path: .ci/canonical-clean-ci-attestation-builder/receipt.json
          predicate-type: https://github.com/jyt6640/persona-harness/attestations/finish-attestation.1
          predicate-path: .ci/canonical-clean-ci-attestation-builder/predicate.json

      - name: Collect signed bundle
        run: cp "${{ steps.attest.outputs.bundle-path }}" .ci/canonical-clean-ci-attestation-builder/bundle.json

      - name: Upload signed builder artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: canonical-clean-ci-attestation-builder
          path: .ci/canonical-clean-ci-attestation-builder
          if-no-files-found: error
          retention-days: 7

      - name: Upload sanitized builder failure diagnostic
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: canonical-clean-ci-attestation-builder-failure
          path: .ci/canonical-clean-ci-attestation-builder/failure-diagnostic.json
          if-no-files-found: ignore
          retention-days: 7
