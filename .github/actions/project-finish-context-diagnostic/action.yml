name: Run bounded project finish context diagnostic
description: Write a bounded diagnostic summary in runner temp before evaluating fixed context.

outputs:
  summary-status:
    description: Bounded status of the persisted diagnostic summary.

runs:
  using: node20
  main: index.mjs
